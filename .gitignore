# dependencies
node_modules/

# build output
dist/
standalone/

# local env (never commit real keys — only .env.example is tracked)
.env
.env.local
.env.*.local

# generated DB seed — contains real forecast data, never commit
seed.sql

# vercel
.vercel/

# editor / OS
.DS_Store
*.log

# local preview config
.claude/
